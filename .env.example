# --- DATABASE SETTINGS ---
# For local docker-compose, these match the 'db' service
POSTGRES_USER=healx
POSTGRES_PASSWORD=secure_local_password_change_me
POSTGRES_DB=healx_vault

# Connection String for the API to talk to DB
# Local Docker: postgresql+asyncpg://healx:secure_local_password_change_me@db:5432/healx_vault
DATABASE_URL=postgresql+asyncpg://healx:secure_local_password_change_me@db:5432/healx_vault

# --- FIREBASE / GOOGLE CLOUD ---
# Your Firebase Storage Bucket Name (without gs://)
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Path to your service account JSON (Only needed locally)
GOOGLE_APPLICATION_CREDENTIALS=./firebase_credentials.json

# --- API CONFIG ---
PORT=8080
